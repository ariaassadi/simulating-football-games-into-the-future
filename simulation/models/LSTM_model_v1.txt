2024-04-01
epochs=1
matches=120
sequence_length=10
numerical_cols=['x', 'y', 'v_x', 'v_y', 'a_x', 'a_y', 'distance_to_ball']
categorical_cols=[]
positions=['Goalkeeper']

Training results:
loss: [1.83]
val_loss: [1.81]

This model actually trained on all positions due to a mistake. It trained on all models and didnt use any embedding layers
